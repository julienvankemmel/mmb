{% extends 'base.html.twig' %}

{% block title %}Backpack{% endblock %}

{% block body %}
    <h1>Backpack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ backpack.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ backpack.name }}</td>
            </tr>
            <tr>
                <th>LastModif</th>
                <td>{{ backpack.lastModif ? backpack.lastModif|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>PublishedDate</th>
                <td>{{ backpack.publishedDate ? backpack.publishedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backpack_index') }}">back to list</a>

    <a href="{{ path('backpack_edit', {'id': backpack.id}) }}">edit</a>

    {{ include('backpack/_delete_form.html.twig') }}
{% endblock %}
