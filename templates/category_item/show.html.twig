{% extends 'base.html.twig' %}

{% block title %}CategoryItem{% endblock %}

{% block body %}
    <h1>CategoryItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_item.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_item_index') }}">back to list</a>

    <a href="{{ path('category_item_edit', {'id': category_item.id}) }}">edit</a>

    {{ include('category_item/_delete_form.html.twig') }}
{% endblock %}
