{% extends 'base.html.twig' %}

{% block title %}CategoryBackpack{% endblock %}

{% block body %}
    <h1>CategoryBackpack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_backpack.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_backpack.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_backpack_index') }}">back to list</a>

    <a href="{{ path('category_backpack_edit', {'id': category_backpack.id}) }}">edit</a>

    {{ include('category_backpack/_delete_form.html.twig') }}
{% endblock %}
