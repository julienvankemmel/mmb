<form method="post" action="{{ path('category_backpack_delete', {'id': category_backpack.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_backpack.id) }}">
    <button class="btn">Delete</button>
</form>
